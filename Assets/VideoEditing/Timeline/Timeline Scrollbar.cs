using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TimelineScrollbar : MonoBehaviour
{
    public Timeline timeline;
    public enum Direction
    {
        left,
        right 
    }
    // when you scroll the timeline, several things happen 

    // the timeline time marker changes (i believe it should be a 2D texture with a collider, where the shader / UX texture moves)
    // 

    // when you hit the left end or right end, depending on where you are, you have to scroll back 

    // you have to update the 
}
